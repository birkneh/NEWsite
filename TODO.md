# TODO: NEWsite

Status key:
- `DONE` completed
- `NEXT` highest-priority upcoming task
- `IN PROGRESS` currently being worked
- `BLOCKED` needs input/decision

## P0 (Immediate)
- `NEXT` Add one featured project detail page per major grant (mpox, ECOVA, PREGART) and link from `/projects/`.
- `NEXT` Add one new blog post per month (maintain freshness on homepage and writing index).
- `NEXT` Verify all external links quarterly (LinkedIn, Google Scholar, PubMed query, CV download).

## P1 (High Impact)
- `NEXT` Build a publications filter (year, keyword, journal) on `/publications/`.
- `NEXT` Add a media/speaking page for invited talks, interviews, and keynotes.
- `NEXT` Add Open Graph and Twitter meta tags for better sharing previews.
- `NEXT` Add a concise \"About\" page with full bio and downloadable short profile.

## P2 (Quality and Growth)
- `NEXT` Add analytics (privacy-friendly) to understand top pages and referral sources.
- `NEXT` Add a sitemap and robots.txt for improved indexing.
- `NEXT` Add a lightweight contact form integration (or clear email CTA alternatives).
- `NEXT` Add a visual timeline of roles and grants.

## Operational
- `DONE` Static GitHub Pages architecture with `.nojekyll`.
- `DONE` Homepage with profile photo and advocacy/research sections.
- `DONE` Writing section with multiple posts and bylines.
- `DONE` Publications page linked to PubMed records.
- `DONE` Projects page and cross-site navigation links.
- `DONE` Codex project handoff file (`CODEX_PROJECT.md`).

## Decisions Needed
- `BLOCKED` Confirm preferred posting cadence (monthly vs biweekly).
- `BLOCKED` Confirm whether to include full phone number publicly on all pages.
- `BLOCKED` Confirm if personal domain should be added (e.g., custom `.org`).
